import Button from '@components/Button/Button';
import { buttonVariants } from "@components/Button/Variants";
import { cn } from '@lib/utils/cn';
import { ComponentCard } from "@/components/ui/ComponentCards/ComponentCard";
import CodePreview from "/components/CodePreview/CodePreview.tsx"

## Button

Displays a button or a component that looks like a button.

## Usage

<CodePreview 

preview={
<div style={{display:"flex", alignItems:"center", justifyContetn:"center", flexWrap:"wrap", gap:"1rem"}}>
{buttonVariants.map((variant) => (
    <ComponentCard key={variant} className>
        <Button variant={variant}>Button</Button>
    </ComponentCard>
))}
</div>
}

code =
{`import Button from '@components/Button/Button';
import {cn} from "@lib/utils/cn";

function ButtonDemo () {
    return (
        <Button variant={cn('primary', 'lg')}>Button</Button>
    );
}
`}

fileName="ButtonDemo.jsx"
language="jsx"/>

## Props

The button component accepts the following props:

| Prop          | Type                           | Description                                                           |
|---------------|---------------------------     |-----------------------------------------------------------------------|
| `children`    | `ReactNode`                    | Content to be displayed inside the button                             |
| `variant`     | `info`, `success`, `warning`,  | The style variant of the button, used to apply different styles       |
|               | `error`, `disabled`, `ghost`,  |                                                                       |
|               | `link`                         |                                                                       |
| `className`   | `string`                       | Additional classes to be added to the button                          |
| `...props`    | `any`                          | Any other props you want to pass to the button                        |
